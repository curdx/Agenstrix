---
phase: 01-first-pty-demo
plan: 06
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified:
  - .github/workflows/ci.yml
  - src-bun/system/win-short-path.ts
  - src-bun/worker/cwd.ts
  - tests/smoke/pty-echo.test.ts
  - tests/smoke/pty-echo-win.test.ts
  - tests/smoke/win-short-path.test.ts
  - README.md
autonomous: false
requirements:
  - INFRA-07
  - INFRA-06

must_haves:
  truths:
    - "GitHub Actions runs the test matrix on `macos-latest`, `ubuntu-latest`, and `windows-latest` with Bun pinned to `1.3.14`; every push and pull_request triggers all three OS runners."
    - "Each runner executes: `bun install` → `bun test tests/unit/` (all unit tests) → `bun test tests/smoke/` (all smoke tests applicable to the platform) → `bunx tsc --noEmit` → `bunx @biomejs/biome check .`."
    - "Windows runner specifically validates: ConPTY-based `Bun.Terminal` spawn of `cmd.exe /c echo hello` produces bytes containing 'hello' within 5 seconds; the kill path on Windows uses `process.kill(pid)` not `-pgid` (verified by smoke test instrumented to check); `getWindowsShortPath` produces a 8.3 short-name for a path containing non-ASCII characters."
    - "POSIX-only tests (kill-group cascade, redactor-pipeline using `sh -c`) are skipped on Windows via `test.skipIf(process.platform === 'win32')`; Windows-only tests are skipped on POSIX."
    - "CI passes on green main branch as the gate before declaring Phase 1 complete; the badge appears in README.md (or is documented as pending in the absence of a publicly-accessible repo)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow with three-OS matrix, Bun setup, full test suite, type-check, lint"
      min_lines: 60
      contains: "matrix"
    - path: "src-bun/system/win-short-path.ts"
      provides: "Windows short-path conversion via `cmd /c for %i in (path) do echo %~si` fallback (per RESEARCH.md A4); pure pass-through on POSIX"
      exports: ["getWindowsShortPath"]
    - path: "tests/smoke/pty-echo.test.ts"
      provides: "Cross-platform PTY echo smoke test (`echo hello` POSIX / `cmd.exe /c echo hello` Windows)"
    - path: "tests/smoke/pty-echo-win.test.ts"
      provides: "Windows-only ConPTY validation: spawn cmd.exe, capture bytes, assert content + kill via `process.kill(pid)`"
    - path: "tests/smoke/win-short-path.test.ts"
      provides: "Windows-only: getWindowsShortPath of a non-ASCII path returns ASCII-only 8.3 form"
    - path: "README.md"
      provides: "Quickstart + CI badge + supported platforms + minimum Bun/Node/Windows versions"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/smoke/*"
      via: "bun test tests/smoke step in each matrix runner"
      pattern: "bun test"
    - from: "src-bun/worker/cwd.ts"
      to: "src-bun/system/win-short-path.ts"
      via: "import { getWindowsShortPath } — already a stub in Plan 01; this plan adds the real cmd-based impl"
      pattern: "getWindowsShortPath"
---

<objective>
Land the INFRA-07 cross-platform invariant: macOS / Linux / Windows 10 1809+ all pass the same smoke suite, with Windows-specific handling for ConPTY byte re-encoding (Pitfall #8 in RESEARCH.md) and the path short-name conversion for non-ASCII cwds (Assumption A4). Validate that the kill-group platform branch (Plan 04) actually works on Windows by exercising the ConPTY cascade in a CI run.

Purpose: INFRA-07 is the Phase 1 invariant most exposed to schedule risk — `Bun.Terminal` Windows ConPTY support shipped only 2026-05-13 (4 days before Phase 1 planning). Local Windows validation is not possible from the macOS dev machine. CI is the primary validation path. Plans 01-05 build Windows-safe code by following the patterns in RESEARCH.md, but only Plan 06 proves it on a real Windows runner.

Output: A green CI matrix that the user (and future contributors) can rely on as the gate for any PR. A README that documents quickstart + supported platforms + version floors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-first-pty-demo/01-RESEARCH.md
@.planning/phases/01-first-pty-demo/01-CONTEXT.md
@.planning/phases/01-first-pty-demo/01-01-SUMMARY.md
@.planning/phases/01-first-pty-demo/01-02-SUMMARY.md
@.planning/phases/01-first-pty-demo/01-03-SUMMARY.md
@.planning/phases/01-first-pty-demo/01-04-SUMMARY.md
@.planning/phases/01-first-pty-demo/01-05-SUMMARY.md
@.planning/phases/01-first-pty-demo/SKELETON.md
@src-bun/worker/cwd.ts
@src-bun/pty/bun-terminal.ts
@package.json

<interfaces>
NEW exports:

From src-bun/system/win-short-path.ts:
```typescript
/** Returns an ASCII-only 8.3 short path for non-ASCII Windows paths.
 *  No-op on POSIX (returns input unchanged).
 *  Uses `cmd /c for %i in ("<path>") do echo %~si` fallback per Pattern 8 / Assumption A4.
 */
export function getWindowsShortPath(longPath: string): string;
```

UPDATED:
- src-bun/worker/cwd.ts: replace the inline stub from Plan 01 with `import { getWindowsShortPath } from "../system/win-short-path"`.
- tests/smoke/pty-echo.test.ts: was created as a placeholder in Plan 01 Task 0b instructions but may not exist yet — this plan creates the canonical version.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-platform PTY echo smoke test + Windows short-path implementation</name>
  <files>tests/smoke/pty-echo.test.ts, tests/smoke/pty-echo-win.test.ts, tests/smoke/win-short-path.test.ts, src-bun/system/win-short-path.ts, src-bun/worker/cwd.ts</files>
  <read_first>
    - .planning/phases/01-first-pty-demo/01-RESEARCH.md (§Pattern 14 CI matrix, §Pattern 8 CWD resolver + Windows short path, §Pitfall 8 ConPTY re-encoding, §Assumption A4)
    - src-bun/worker/cwd.ts (current stub from Plan 01)
    - src-bun/pty/bun-terminal.ts (final platform-branched kill from Plan 04)
    - src-bun/worker/index.ts (final spawnWorker with test-argv-override from Plan 05)
  </read_first>
  <behavior>
    Cross-platform smoke test (tests/smoke/pty-echo.test.ts):
    - Spawn `cli: "echo-skeleton"` with `_testArgvOverride = process.platform === "win32" ? ["cmd.exe", "/c", "echo hello & timeout /t 5 /nobreak >NUL"] : ["sh", "-c", "echo hello; sleep 5"]`.
    - Within 3 seconds, collect bus events for `worker.output.<id>`; concatenate; decode UTF-8; assert contains the string `"hello"`.
    - On Windows: also assert the platform-branched `kill()` works — call `killWorker(id, graceful=false)`, wait 2s, assert `isProcessAlive(workerPid) === false`.
    - On POSIX: confirm group kill works — covered already by Plan 04's tests/smoke/kill-group.test.ts; this test asserts only the bytes flow.
    
    Windows-only test (tests/smoke/pty-echo-win.test.ts):
    - `test.skipIf(process.platform !== "win32")`.
    - Spawn `cmd.exe /c echo hello`. Capture bytes. Assert bytes contain `hello\r\n` (Windows CRLF — note POSIX would be just `\n`; this asserts the platform behavior is real, not a Bun normalization).
    - Spawn `cmd.exe /c "ver"` (prints Windows version). Capture bytes. Assert no exception was thrown and bytes were received.
    - Note about ConPTY re-encoding (Pitfall #8): do NOT assert byte-for-byte identity with POSIX echo output. Assert semantic content only.
    
    Windows-short-path test (tests/smoke/win-short-path.test.ts):
    - `test.skipIf(process.platform !== "win32")`.
    - Create a tmpdir with a non-ASCII name: `mkdtempSync(join(os.tmpdir(), "agenstrix-测试-"))`.
    - Call `getWindowsShortPath(tmpdir)`. Assert: result contains only ASCII bytes (`/^[\x00-\x7F]+$/`). Assert: result is a valid path (try `existsSync(result)` → true).
    - Cleanup tmpdir.
  </behavior>
  <action>
    Step 1 — write `src-bun/system/win-short-path.ts`:
        ```
        import { execSync } from "node:child_process";
        
        export function getWindowsShortPath(longPath: string): string {
          if (process.platform !== "win32") return longPath;
          if (/^[\x00-\x7F]+$/.test(longPath)) return longPath;  // ASCII-only: no conversion needed
          try {
            // cmd.exe trick: `for %i in ("path") do echo %~si` prints the short name
            // Escape double-quotes in path; PowerShell or special chars not supported in Phase 1
            const escaped = longPath.replace(/"/g, '""');
            const out = execSync(\`cmd /c for %i in ("\${escaped}") do @echo %~si\`, {
              encoding: "utf8",
              windowsHide: true,
            }).trim();
            // execSync may return multi-line if iteration matched more than one; take first non-empty
            const firstLine = out.split(/\r?\n/).find(l => l.length > 0);
            return firstLine || longPath;
          } catch (e) {
            // Best-effort: log via diagnosticsLogger and return original
            return longPath;
          }
        }
        ```
        Note: this uses `execSync` (synchronous child_process — acceptable for cwd resolution at spawn time; not in a hot path).
    Step 2 — modify `src-bun/worker/cwd.ts` to import and call `getWindowsShortPath` (replacing the inline stub Plan 01 had):
        ```
        import { getWindowsShortPath } from "../system/win-short-path";
        // ... in resolveCwd, replace the Windows branch:
        if (process.platform === "win32") {
          return getWindowsShortPath(resolved);
        }
        ```
    Step 3 — write `tests/smoke/pty-echo.test.ts` per the cross-platform contract above. Use `_testArgvOverride` from Plan 05 to inject the platform-specific argv. Test timeout 15s.
    Step 4 — write `tests/smoke/pty-echo-win.test.ts` (Windows-only). Note: this test cannot be run locally on macOS — it will be exercised by CI. Still write it; CI is the validation.
    Step 5 — write `tests/smoke/win-short-path.test.ts` (Windows-only). Same caveat.
    Step 6 — locally (macOS), run `bun test tests/smoke/pty-echo.test.ts` — POSIX branch executes and should pass. The Windows-only tests are skipped (skipIf shows `(skipped)` in output).
    Step 7 — `bunx tsc --noEmit` (must work cross-platform — the `execSync` import is from `node:child_process` which Bun supports).
  </action>
  <verify>
    <automated>bun test tests/smoke/pty-echo.test.ts && grep -c 'getWindowsShortPath' src-bun/worker/cwd.ts | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c 'cmd /c for' src-bun/system/win-short-path.ts | awk '$1 >= 1 {exit 0} {exit 1}' && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/smoke/pty-echo.test.ts` passes locally on POSIX (`bun test` confirms)
    - Windows-only test files exist with `test.skipIf(process.platform !== 'win32')` and are syntactically valid
    - `src-bun/system/win-short-path.ts` exports `getWindowsShortPath` and ASCII paths short-circuit early
    - `src-bun/worker/cwd.ts` imports and calls the new helper
    - `bunx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Cross-platform test scaffold ready; Windows-specific concerns coded but pending CI validation.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI matrix (macOS / Linux / Windows) + README quickstart</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>
    - .planning/phases/01-first-pty-demo/01-RESEARCH.md (§Pattern 14 CI matrix YAML, §Pitfall 9 bun upgrade version pinning)
    - .planning/PROJECT.md (constraints — Bun-only, Windows 10 1809+, MIT license)
    - .planning/phases/01-first-pty-demo/SKELETON.md (Stack Touched table — what CI must validate)
    - package.json (current scripts to invoke from CI)
  </read_first>
  <action>
    Step 1 — `mkdir -p .github/workflows` (use `mkdir -p`, not `mkdir`, for idempotency on re-run).
    Step 2 — write `.github/workflows/ci.yml`:
        ```yaml
        name: CI
        on:
          push:
            branches: [main]
          pull_request:
            branches: [main]
        jobs:
          test:
            strategy:
              fail-fast: false
              matrix:
                os: [macos-latest, ubuntu-latest, windows-latest]
            runs-on: ${{ matrix.os }}
            timeout-minutes: 15
            steps:
              - uses: actions/checkout@v4
        
              - name: Setup Bun
                uses: oven-sh/setup-bun@v2
                with:
                  bun-version: "1.3.14"
        
              - name: Install dependencies
                run: bun install --frozen-lockfile
        
              - name: Type check
                run: bunx tsc --noEmit
        
              - name: Lint
                run: bunx @biomejs/biome check .
        
              - name: Unit tests
                run: bun test tests/unit/
        
              - name: Smoke tests (POSIX)
                if: runner.os != 'Windows'
                run: bun test tests/smoke/
        
              - name: Smoke tests (Windows)
                if: runner.os == 'Windows'
                shell: pwsh
                run: bun test tests/smoke/
        ```
        Note: the only POSIX-vs-Windows difference is shell choice (`pwsh` on Windows for env-handling correctness). Test files themselves handle platform branching via `skipIf`.
    Step 3 — write `README.md` (greenfield — create from scratch):
        ```markdown
        # Agenstrix
        
        多智能体 CLI 编排应用 — 自主 Master + 真 PTY 多 CLI Worker + 智能 workspace
        
        > _Phase 1 work-in-progress: Walking Skeleton + real `claude` PTY in the browser._
        
        ![CI](https://github.com/<owner>/<repo>/actions/workflows/ci.yml/badge.svg)
        
        ## What is this?
        
        Agenstrix runs a real interactive `claude` CLI on your machine as the "Master" and lets it autonomously
        spawn more `claude` / `codex` workers in independent git worktrees. You drag a few project folders into
        Agenstrix and it auto-detects everything — no config files.
        
        See [.planning/PROJECT.md](./.planning/PROJECT.md) for the full vision.
        
        ## Quickstart (Phase 1)
        
        Prereqs:
        - **Bun** >= 1.3.14 — `curl -fsSL https://bun.sh/install | bash -s "bun-v1.3.14"`
        - **Node** >= 20 (for Vite tooling)
        - **git** on PATH
        - **claude** CLI on PATH — `npm install -g @anthropic-ai/claude-code`
        - **Platforms:** macOS, Linux, Windows 10 1809+ (Windows requires Bun.Terminal ConPTY, shipped 2026-05-13)
        
        Install + run:
        
            git clone <repo>
            cd Agenstrix
            bun install
            bun run dev
        
        Visit http://localhost:5173. You should see the chat shell with a live `claude` PTY in the browser.
        
        ## Subcommands
        
        - `bunx agenstrix` (or `bunx agenstrix start`) — Start the backend + open the browser.
        - `bunx agenstrix --port 4001` — Override the default port (3000).
        - `bunx agenstrix doctor --reap` — Scan `~/.agenstrix/running.json` for orphan PTY processes from previous crashed runs, prompt to kill.
        - `bunx agenstrix doctor --reap --yes` — Non-interactive (CI).
        
        ## Data location
        
        Everything Agenstrix learns lives in `~/.agenstrix/`:
        - `store.db` — SQLite (Drizzle ORM), WAL mode
        - `backups/` — pre-migration DB backups (keeps last 10)
        - `logs/agenstrix-YYYY-MM-DD.log` — user-facing log
        - `logs/diagnostics-YYYY-MM-DD.log` — internal trace log
        - `running.json` — live PID tracker for doctor --reap
        
        **No yaml. No JSON config files. Ever.**
        
        ## Development
        
        - `bun test` — full test suite (unit + smoke)
        - `bunx tsc --noEmit` — type check
        - `bunx @biomejs/biome check .` — lint
        - `bun run db:generate` — regenerate Drizzle SQL after schema changes (review the SQL before committing)
        
        ## License
        
        MIT — see [LICENSE](./LICENSE).
        ```
        Replace `<owner>/<repo>` with the actual repo path if known; otherwise the badge will 404 harmlessly until first push.
    Step 4 — sanity: the workflow file is valid YAML (use any local validator or trust the CI to fail loudly if invalid). `bun run type-check` and `bunx @biomejs/biome check` still pass.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c 'matrix:' .github/workflows/ci.yml | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c 'macos-latest' .github/workflows/ci.yml | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c 'windows-latest' .github/workflows/ci.yml | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c '1.3.14' .github/workflows/ci.yml | awk '$1 >= 1 {exit 0} {exit 1}' && test -f README.md && grep -c 'bunx agenstrix' README.md | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists with three-OS matrix and Bun 1.3.14 pinned
    - CI runs: install → tsc → biome → unit tests → smoke tests (per platform)
    - README.md exists with quickstart, prereqs, subcommands, data location, license
    - `bunx tsc --noEmit` still passes locally
    - `bunx @biomejs/biome check .` still passes locally
  </acceptance_criteria>
  <done>CI workflow file written; README documents quickstart. Validation happens on first push (next task).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human pushes to GitHub, observes CI run across all three OS runners, confirms green</name>
  <what-built>The GitHub Actions workflow needs to be exercised against the actual `windows-latest` runner to validate Bun.Terminal ConPTY (since the dev machine is macOS). All other plans' tests are validated by this CI run.</what-built>
  <how-to-verify>
    1. **Pre-flight (local):** Run `bun test tests/unit/ tests/smoke/` on your dev machine. Confirm all POSIX-applicable tests pass and Windows-only tests show as `(skipped)`. If anything fails, fix it BEFORE pushing.
    2. **Pre-flight (lint+type):** `bunx tsc --noEmit && bunx @biomejs/biome check .` both exit 0.
    3. **Stage + commit:** `git add .github/workflows/ci.yml README.md src-bun/system/win-short-path.ts src-bun/worker/cwd.ts tests/smoke/pty-echo.test.ts tests/smoke/pty-echo-win.test.ts tests/smoke/win-short-path.test.ts && git commit -m "ci(01-06): GitHub Actions matrix + Windows short-path + README"`.
    4. **Push:** `git push origin main` (or to the appropriate branch; if PR-only, open a PR).
    5. **Watch CI:** `gh run watch` (or open the Actions tab in the GitHub web UI). Confirm all three jobs (macOS / Linux / Windows) complete.
    6. **Validate Windows-specific gates:**
        - The `pty-echo.test.ts` test PASSES on Windows (proves `Bun.Terminal` ConPTY spawn works).
        - The `pty-echo-win.test.ts` test PASSES on Windows (proves the Windows-specific kill path works AND ConPTY re-encoding is captured without crashing).
        - The `win-short-path.test.ts` test PASSES on Windows (proves the `cmd /c for %i in (...) do echo %~si` fallback resolves non-ASCII paths).
    7. **If Windows tests fail:** the most likely cause is one of:
        (a) Bun 1.3.14 ConPTY has an unreported bug on the Windows runner — the fallback plan per CONTEXT.md is to swap `Bun.Terminal` for `bun-pty` (5-line change in `src-bun/pty/handle.ts` factory). Test by setting `AGENSTRIX_PTY_BACKEND=bun-pty` env var in the Windows CI step temporarily.
        (b) `getWindowsShortPath` cmd quoting wrong → inspect the failing test output, fix the quoting, push again.
        (c) Path uses `/` vs `\` inconsistency → ensure tests use `path.join`, not literal `/`.
    8. **Reply with one of:**
        - `approved` — all three OS runners green; Phase 1 is shippable
        - `partial-mac-linux` — POSIX runners green, Windows failing; plan a fallback strategy (set bun-pty as Windows-only default? defer Windows to Phase 2?) — capture as a follow-up issue
        - `block` — fundamental CI issue requires triage
  </how-to-verify>
  <on-block>If Windows ConPTY is fundamentally broken in Bun 1.3.14: the Phase 1 success criteria say Windows must pass. Options ranked: (1) wire `bun-pty` as the Windows-default backend in `src-bun/pty/handle.ts` factory (`if (process.platform === "win32") return createBunPtyFallback(opts)`) — this is a 5-line change; verify bun-pty's API matches the assumed signature (RESEARCH.md A2 needs verification). (2) If bun-pty also fails on Windows, escalate to user: Windows support deferred to Phase 1.5 (separate sub-phase) with documented limitation in README.</on-block>
  <resume-signal>Type "approved" with the URL of the green CI run, or "partial-mac-linux" with notes for follow-up.</resume-signal>
  <files>(checkpoint task — no files modified by Claude; human action only)</files>
  <action>This is a checkpoint:human-verify task. The work to verify is described in <what-built> above and the verification steps are in <how-to-verify>. Claude must STOP execution here and surface the <how-to-verify> content to the user, then wait for the <resume-signal> response before proceeding.</action>
  <verify><human-check>See <how-to-verify> block above for the exact steps the human must run.</human-check></verify>
  <done>Human responds with the resume-signal (typically "approved" or a list of failing items for gap closure).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions secrets → CI runners | Phase 1 CI does not need secrets (no deploy, no signing); standard `GITHUB_TOKEN` only |
| Test runners → host filesystem | Tests use `os.tmpdir()`; CI runners are ephemeral; no persistent risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | Malicious dep injected via `bun install` on CI (supply chain) | mitigate | `bun install --frozen-lockfile` enforces `bun.lock` exact versions; combined with Task 0a human-verify gate on [ASSUMED] packages |
| T-01-06-02 | Denial of Service | CI runner timeout (15 min) on Windows due to ConPTY slow boot | accept | If hit, increase timeout or move slow smoke test to a `windows-only` job with longer timeout |
| T-01-06-03 | Information Disclosure | CI logs leaking dev paths or env vars | accept | No secrets in CI; `process.env` of CI runner is sanitized by Actions; no `set -x` style verbose shell |
</threat_model>

<success_criteria>
- CI passes on all three OS runners
- Bun pinned to exactly 1.3.14 in CI (per Pitfall #9 — don't allow `bun upgrade` drift)
- Windows ConPTY validated by `pty-echo-win.test.ts` AND `win-short-path.test.ts`
- README.md documents quickstart + prereqs + subcommands
- INFRA-07 satisfied: cross-platform tested
- INFRA-06 deepened: cross-platform self-test validated indirectly via the smoke suite booting on each OS
</success_criteria>

<output>
Create `.planning/phases/01-first-pty-demo/01-06-SUMMARY.md`:
- CI matrix structure + Bun version pinning rationale
- Windows-specific tests + skipIf strategy
- getWindowsShortPath cmd-based fallback details + when to swap for FFI in Phase 2
- Fallback plan if Windows ConPTY proves unreliable: bun-pty as Windows-default (5-line factory change)
- Phase 1 closing: all 17 requirement IDs now satisfied — mark in REQUIREMENTS.md traceability
</output>
